# ACE Tool Configuration

# ACE CLI Tool Path (optional, if not in PATH)
ACE_CLI_PATH="/path/to/ace-tool"

# ACE MCP Server Base URL
ACE_BASE_URL="https://your-ace-server.com/relay"

# ACE MCP Server API Key
ACE_API_KEY="your-ace-api-key-here"

# Port for ACE Daemon (后台守护进程 + Web UI)
ACE_PORT=4231

# Timeout Configuration
# ACE_HEARTBEAT_TIMEOUT_MINUTES: 心跳超时（分钟），默认 120 分钟（2 小时）
# 设置为 0 表示禁用心跳自动退出
ACE_HEARTBEAT_TIMEOUT_MINUTES=120

# ACE_RPC_TIMEOUT_SECONDS: RPC 超时（秒），默认 300 秒（5 分钟）
# 建议值：300（5分钟）-3600（1小时）
# 超小值：30-60 秒（测试用）
# 零大值：3600+ 秒（长时间任务）
ACE_RPC_TIMEOUT_SECONDS=300