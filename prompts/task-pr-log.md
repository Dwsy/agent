---
description: 将本次任务留档到 docs/pr/日期-xxx.md
---

请为本次任务创建一份 PR 留档文档，写入 `docs/pr/YYYY-MM-DD-xxx.md`。

要求：
- 文件名中的 `YYYY-MM-DD` 使用当天日期。
- `xxx` 使用能概括本次任务的短横线命名标题。
- 内容记录本次任务的目标、主要变更、涉及文件、验证结果和后续事项。
- 如果 `docs/pr/` 不存在，先创建目录。
- 留档应基于真实变更和已执行验证，不要编造未发生的测试或结果。
